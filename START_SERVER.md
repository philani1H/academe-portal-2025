# How to Start the Server

## The Error You're Seeing

```
Error: connect ECONNREFUSED 127.0.0.1:3000
```

This means the **backend server is not running**. The frontend (Vite on port 8080) is trying to connect to the backend (!
artto sth servers or botwait fnd run dev` anpm Just run `mand**: k Com

**Quicics

---tiststads with ard loa
- DashbororsREFUSED er ECONN3000
- Nost:lhop://locaackend: httst:8080
- B/localhoend: http:/ront- Fou'll see:
rking, y!

When wo
## Success1
   ```
 PORT=300`env
  env`:
   ``n `. iorterent p Try a diffewall
5.iry f blocked b00 isk if port 30sts
4. Checdex.ts` exirver/inserc/3. Verify `sdirectory
 root  project're in the sure you Makees
2.or messagerrfor l namithe ter

1. Check ? Working Still Not

##`
``45    123ENINGST0    LI.0.0.0:000    00:3    0.0.0.
# TCPng like:somethiw # Should sho

dstr :3000| finstat -ano Windows
net
```bash
# Running
is ver eck if Serh.

## Chstart botd houlcommand s dev`  run`npm

The 000- Port 3** ess)nd (Expr. **BackePort 8080
2)** - tetend (Vi**Froncesses:
1. ed TWO proneg

You Runninhould Be 
## What S   ```
 dev
   npm run  ```bash
resh:
 rt f3. Sta  ```
 node.exe
 ll /F /IM
   taskki Windows  #bash
 ses:
   ```uck procesear any st Clrl+C)
2.Ctsses ( procel runningop al
1. St
ess ProcartFull Rest

## build
```n pm rush
n
```baors:l see errilstyou n fixed. If as bee herver fileors
The sript Errue 4: TypeScIss
```

### pm installbash
ncies
```ng Dependensue 3: Missi### Is```

e.db"
abasP-Dat:./APileURL="fv
DATABASE_:
```en file has `.env`
Check yourorrrion Ennecttabase Co: Da 2### Issue
```

MBER> /FID_NUll /PID <P000
taskkir :3ano | findststat -
net port 3000s oncesill proFind and kindows - ash
# W``bdy in Use
` Alrea3000Port ## Issue 1: 
#ssues
# Common Ics.

#th statistiSON wi Jturn

Should re```ats
dmin/stapi/a3000/t:localhoshttp://bash
curl :
```se curlr or u your browseAPI

Openst the Te
## ```
ted
ecase connDatabrt 3000
nning on po`
Server rue:
``should sed, you startence ning

Or is Runervey S# Verif

#
}
```
  }"e buildsc && vit "tuild": "b",
   vite: " "client",
   r/index.ts"veh src/ser"tsx watcer": 
    "serv"",ent\cliun " \"npm r server\pm run"ntly \urren"concdev":    "": {
 ts
  "scripon
{ke:

```jsng lihi have somet. You shouldsectionscripts or the ok fon` and lo`package.js

Open n Scriptsge.jsoackaheck p Cn 3:Optio## ``

#x.ts
`er/inderc/serv
npx tsx sash tsx:
```b
Or withs
```
r/index.t/serveh
node src``bas
`esn't work:that do`

Or if 
``run serverm sh
npOnly
```baart Backend : St## Option 2gether.

#ackend tod bend anont BOTH the frhould startThis s
```

un devm r`bash
npmmended)
``g npm (Recon 1: Usintio Op###er

rvSeart the Stix - ick F Qu##ed.

tartt's not s, but i0)on port 300Express 